<x-guest-layout>
  <div class="min-h-screen bg-black flex items-center justify-center p-4">
    <div class="w-full max-w-md bg-black rounded-2xl p-8 border border-gray-800">
      <header class="mb-8 text-center">
        <h1 class="text-2xl font-bold text-white mb-2">
          Crea tu Tienda
        </h1>
        <p class="text-gray-400 text-sm">
          Configura el nombre, logo y portada.<br>
          Podrás editarlos más adelante.
        </p>
      </header>

      @if ($errors->any())
        <div class="mb-6 rounded-lg bg-red-500/15 text-red-300 border border-red-500/30 px-4 py-3 text-sm">
          <ul class="list-disc pl-5">
            @foreach ($errors->all() as $error)
              <li>{{ $error }}</li>
            @endforeach
          </ul>
        </div>
      @endif

      <form method="POST" action="{{ route('store.store') }}" enctype="multipart/form-data" class="space-y-6">
        @csrf

        <div>
          <label class="block text-sm font-medium text-white mb-2">Nombre de la tienda *</label>
          <input type="text" name="name" required
            class="w-full rounded-lg bg-white text-black placeholder-gray-500 border-0 focus:ring-2 focus:ring-gray-600 px-4 py-3 text-sm" placeholder="Ej. Moto Repuestos Arenas" />
        </div>

        <div>
          <label class="block text-sm font-medium text-white mb-2">Descripción</label>
          <textarea name="description" rows="3"
            class="w-full rounded-lg bg-white text-black placeholder-gray-500 border-0 focus:ring-2 focus:ring-gray-600 px-4 py-3 text-sm resize-none"
            placeholder="Breve descripción de tu tienda, categoría, servicios..."></textarea>
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium text-white mb-2">Logo</label>
            <label class="block text-sm font-medium text-gray-400 mb-2">(archivo)</label>
            <input type="file" name="logo" accept="image/*"
              class="w-full text-xs text-gray-400 file:mr-2 file:py-1 file:px-2 file:rounded file:border-0 file:text-xs file:bg-gray-700 file:text-white" />
            <label class="block text-sm font-medium text-gray-400 mt-2">o URL del logo</label>
            <input type="url" name="logo_url" placeholder="https://..."
              class="w-full rounded-lg bg-white text-black placeholder-gray-500 border-0 focus:ring-2 focus:ring-gray-600 px-3 py-2 text-sm mt-1" />
          </div>

          <div>
            <label class="block text-sm font-medium text-white mb-2">Portada/Fondo</label>
            <label class="block text-sm font-medium text-gray-400 mb-2">(archivo)</label>
            <input type="file" name="cover" accept="image/*"
              class="w-full text-xs text-gray-400 file:mr-2 file:py-1 file:px-2 file:rounded file:border-0 file:text-xs file:bg-gray-700 file:text-white" />
            <label class="block text-sm font-medium text-gray-400 mt-2">o URL de portada</label>
            <input type="url" name="cover_url" placeholder="https://..."
              class="w-full rounded-lg bg-white text-black placeholder-gray-500 border-0 focus:ring-2 focus:ring-gray-600 px-3 py-2 text-sm mt-1" />
          </div>
        </div>

        <div class="flex gap-3 pt-4">
          <a href="{{ url('/') }}"
            class="flex-1 inline-flex items-center justify-center px-4 py-3 rounded-lg bg-white text-black font-semibold text-sm hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-600">
            Cancelar
          </a>
          <button type="submit"
            class="flex-1 inline-flex items-center justify-center px-4 py-3 rounded-lg bg-black text-white font-semibold text-sm border border-gray-700 hover:bg-gray-900 focus:outline-none focus:ring-2 focus:ring-gray-600">
            Guardar y continuar
          </button>
        </div>
      </form>
    </div>
  </div>
</x-guest-layout>
